{% extends 'base.html.twig' %}

{% block title %}
    Contacte les Déesses
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-4">
                <img src="{{ asset('images/contact.png') }}" alt="Contact" class="img-fluid">
            </div>
            <div class="col-md">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-custom-primary w-100">
                    Et hop, tu envoie
                </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

